spring:
  datasource:
    url: jdbc:mysql://localhost:3306/wit?serverTimezone=Asia/Seoul&characterEncoding=utf8
    username: ssafy
    password: ssafy
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true

  data:
    redis:
      host: localhost
      port: 6379

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
